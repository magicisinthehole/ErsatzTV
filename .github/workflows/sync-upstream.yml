name: Sync with Upstream and Build

on:
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      force_build:
        description: 'Build even if not behind upstream?'
        type: boolean
        default: false
      platforms:
        description: 'Platforms to build (comma-separated)'
        type: string
        default: 'linux/amd64,linux/arm64'

jobs:
  sync-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    env:
      # Use workflow input on manual runs; default to amd64+arm64 otherwise
      PLATFORMS: ${{ github.event_name == 'workflow_dispatch' && inputs.platforms || 'linux/amd64,linux/arm64' }}
      SHOULD_BUILD: ${{ steps.check_changes.outputs.behind > 0 || (github.event_name == 'workflow_dispatch' && fromJSON(inputs.force_build)) }}

    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          ref: feature/native-scale-support
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/ersatztv/ersatztv.git || true
          git fetch upstream

      - name: Check for upstream changes
        id: check_changes
        run: |
          git fetch upstream main
          BEHIND=$(git rev-list --count HEAD..upstream/main)
          echo "behind=$BEHIND" >> $GITHUB_OUTPUT
          echo "Commits behind upstream: $BEHIND"

      - name: Merge upstream changes
        if: ${{ steps.check_changes.outputs.behind > 0 }}
        run: |
          echo "Merging upstream/main into feature/native-scale-support"
          git merge upstream/main --no-edit || {
            echo "Merge conflict detected!"
            echo "::warning::Manual intervention required - merge conflicts with upstream"
            exit 1
          }

      - name: Push merged changes
        if: ${{ steps.check_changes.outputs.behind > 0 }}
        run: git push origin feature/native-scale-support

      # NEW: QEMU is required for reliable linux/arm64 under emulation
      - name: Set up QEMU
        if: ${{ env.SHOULD_BUILD == 'true' }}
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        if: ${{ env.SHOULD_BUILD == 'true' }}
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        if: ${{ env.SHOULD_BUILD == 'true' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: ${{ env.SHOULD_BUILD == 'true' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile
          platforms: ${{ env.PLATFORMS }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/ersatztv:scale-support
            ghcr.io/${{ github.repository_owner }}/ersatztv:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        if: ${{ env.SHOULD_BUILD == 'true' }}
        run: |
          echo "✅ Successfully synced with upstream and rebuilt Docker image" >> $GITHUB_STEP_SUMMARY
          echo "📦 Image: ghcr.io/${{ github.repository_owner }}/ersatztv:scale-support" >> $GITHUB_STEP_SUMMARY

      - name: No changes
        if: ${{ env.SHOULD_BUILD != 'true' }}
        run: echo "✅ Already up to date with upstream" >> $GITHUB_STEP_SUMMARY
